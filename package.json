{
  "name": "launchpad",
  "version": "1.0.0",
  "description": "Local dashboard to track GitHub repo deploy status",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
